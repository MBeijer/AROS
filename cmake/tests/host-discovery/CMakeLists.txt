cmake_minimum_required(VERSION 3.20)
project(AROSHostDiscoveryFixture C)
include("${AROS_CMAKE_DIR}/AROSModule.cmake")
include("${AROS_CMAKE_DIR}/AROSHostTargets.cmake")
include("${AROS_CMAKE_DIR}/AROSScriptOutputs.cmake")
set(AROS_TARGET linux-x86_64)
set(AROS_TARGET_FAMILY unix)
set(AROS_NATIVE_OUTPUT_ROOT "${CMAKE_BINARY_DIR}/native")
set(AROS_NATIVE_SYSTEM_DIR "${AROS_NATIVE_OUTPUT_ROOT}/runtime")
set(AROS_NATIVE_INCLUDE_DIR "${CMAKE_BINARY_DIR}/include")
aros_collect_mmake_manifests(AROS_MMAKE_MANIFESTS)
aros_add_mmake_host_target(launcher MMAKE_NAME example-launcher)
aros_add_mmake_script_output(boot-config MMAKE_NAME example-boot CONFIGURE_PREVIEW)
get_target_property(_preview boot-config AROS_SCRIPT_PREVIEW)
file(READ "${_preview}" _contents)
file(GENERATE OUTPUT "${CMAKE_BINARY_DIR}/selected-config" CONTENT "${_contents}")
add_custom_target(check ALL DEPENDS launcher boot-config)
